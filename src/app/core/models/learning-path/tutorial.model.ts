export interface ITutorial {
  id: number;
  number: number;
  moduleId: number;
  title: string;
  durationSeconds: number;
  videoUrl: string;
  startFilesUrl: string;
  finishedFilesUrl: string;
}
